openapi: 3.0.3
info:
  title: СистемаКонтроля API
  version: "1.0.0"
servers:
  - url: /api/v1
paths:
  /auth/register:
    post:
      summary: Register user
  /auth/login:
    post:
      summary: Login
  /users:
    get:
      summary: List users (admin)
  /orders:
    post:
      summary: Create order
